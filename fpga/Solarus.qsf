# --------------------------------------------------------------------------
#
# MiSTer project
#
# WARNING WARNING WARNING:
# Do not add files to project in Quartus IDE! It will mess this file!
# Add the files manually to files.qip file.
#
# --------------------------------------------------------------------------

set_global_assignment -name VERILOG_MACRO "MENU_CORE=1"
set_global_assignment -name VERILOG_MACRO "SOLARUS_CORE=1"
# Solarus never sets MISTER_FB (no HPS-framebuffer palette mode -- video is
# direct RGB through the FPGA compositor), so ascal's default-on PALETTE
# generic (pal1_mem) is dead logic that was still eating routing budget and
# driving the pll_hdmi divclk domain negative (60fps campaign Phase 3a STA
# gate, ~-1.0ns across a 10-seed sweep). Drop it.
set_global_assignment -name VERILOG_MACRO "MISTER_DISABLE_PALETTE1=1"

set_global_assignment -name TOP_LEVEL_ENTITY sys_top
set_global_assignment -name PARTITION_NETLIST_TYPE SOURCE -section_id Top
set_global_assignment -name PARTITION_FITTER_PRESERVATION_LEVEL PLACEMENT_AND_ROUTING -section_id Top
set_global_assignment -name PARTITION_COLOR 16764057 -section_id Top

set_global_assignment -name LAST_QUARTUS_VERSION "17.0.0 Lite Edition"

set_global_assignment -name GENERATE_RBF_FILE ON
set_global_assignment -name PROJECT_OUTPUT_DIRECTORY output_files
set_global_assignment -name NUM_PARALLEL_PROCESSORS ALL
set_global_assignment -name SAVE_DISK_SPACE OFF
set_global_assignment -name SMART_RECOMPILE ON
set_global_assignment -name MIN_CORE_JUNCTION_TEMP "0"
set_global_assignment -name MAX_CORE_JUNCTION_TEMP 85
set_global_assignment -name POWER_PRESET_COOLING_SOLUTION "23 MM HEAT SINK WITH 200 LFPM AIRFLOW"
set_global_assignment -name POWER_BOARD_THERMAL_MODEL "NONE (CONSERVATIVE)"
set_global_assignment -name TIMEQUEST_MULTICORNER_ANALYSIS OFF
set_global_assignment -name OPTIMIZE_POWER_DURING_FITTING OFF
set_global_assignment -name FINAL_PLACEMENT_OPTIMIZATION ALWAYS
set_global_assignment -name FITTER_EFFORT "STANDARD FIT"
set_global_assignment -name OPTIMIZATION_MODE "HIGH PERFORMANCE EFFORT"
set_global_assignment -name ALLOW_POWER_UP_DONT_CARE ON
set_global_assignment -name QII_AUTO_PACKED_REGISTERS "SPARSE AUTO"
set_global_assignment -name ROUTER_LCELL_INSERTION_AND_LOGIC_DUPLICATION ON
set_global_assignment -name PHYSICAL_SYNTHESIS_COMBO_LOGIC ON
set_global_assignment -name PHYSICAL_SYNTHESIS_EFFORT EXTRA
set_global_assignment -name PHYSICAL_SYNTHESIS_REGISTER_DUPLICATION ON
set_global_assignment -name PHYSICAL_SYNTHESIS_REGISTER_RETIMING ON
set_global_assignment -name OPTIMIZATION_TECHNIQUE SPEED
set_global_assignment -name MUX_RESTRUCTURE ON
set_global_assignment -name REMOVE_REDUNDANT_LOGIC_CELLS ON
set_global_assignment -name AUTO_DELAY_CHAINS_FOR_HIGH_FANOUT_INPUT_PINS ON
set_global_assignment -name PHYSICAL_SYNTHESIS_COMBO_LOGIC_FOR_AREA ON
set_global_assignment -name ADV_NETLIST_OPT_SYNTH_WYSIWYG_REMAP ON
set_global_assignment -name SYNTH_GATED_CLOCK_CONVERSION ON
set_global_assignment -name PRE_MAPPING_RESYNTHESIS ON
set_global_assignment -name ROUTER_CLOCKING_TOPOLOGY_ANALYSIS ON
set_global_assignment -name ECO_OPTIMIZE_TIMING ON
set_global_assignment -name PERIPHERY_TO_CORE_PLACEMENT_AND_ROUTING_OPTIMIZATION ON
set_global_assignment -name PHYSICAL_SYNTHESIS_ASYNCHRONOUS_SIGNAL_PIPELINING ON
set_global_assignment -name ALM_REGISTER_PACKING_EFFORT LOW
set_global_assignment -name OPTIMIZE_POWER_DURING_SYNTHESIS OFF
set_global_assignment -name ROUTER_REGISTER_DUPLICATION ON
set_global_assignment -name FITTER_AGGRESSIVE_ROUTABILITY_OPTIMIZATION ALWAYS
set_global_assignment -name SEED 1

source sys/sys.tcl
source sys/sys_analog.tcl
source files.qip

# Solarus user CDC clock-group constraints (timing-closure fix).
set_global_assignment -name SDC_FILE Solarus.sdc

# [#46] IOB-pack the SDRAM DQ read-capture register (jtframe burst path). The
# port-wildcard form in sys.tcl (FAST_INPUT_REGISTER -to SDRAM_DQ[*]) is silently
# IGNORED by the fitter (warnings 176250/176251), so our capture flop
# (jtframe_burst_io: dout <= sdram_dq) was left UNPACKED in the fabric on a long,
# place/route-variable interconnect -> marginal setup on the first beat after each
# SDRAM burst restart -> the flickering 64px scanout seam (#46, read-path; the
# multicycle SDC made STA report clean but never fixed the physical capture).
# Mirror jtcores' targeted pack (mister.qsf:65 targets the classic dq_ff).
# IMPORTANT: Quartus IGNORES *wildcard* fast-I/O-register destinations (warning
# 176251 — this is exactly why the SDRAM_DQ[*] port-wildcard never took, and why a
# first "*...|dout[*]" attempt also failed: fit kept the warnings and dout stayed
# core-side, duplicated dout~N, fed via Pad-To-Core delay chains). Use the EXACT
# fully-qualified node path, no wildcards, base name like jotego's dq_ff. Pure
# constraint — no RTL/latency change. Verify post-build: 176250/176251 gone + the
# Register Packing report lists this node as "Packed Register / Fast Input Register".
set_instance_assignment -name FAST_INPUT_REGISTER ON -to "emu:emu|sdram_fb_cache:fbcache|jtframe_burst_sdram:u_sdram_ctrl|jtframe_burst_io:u_io|dout" -entity sys_top

# [ARGB4444 plane bake -- LAB-overflow diagnostic, Task 3 bisection] Making
# bgw_argb4444 (blitter_top.sv) a real, non-constant signal instead of the
# always-0 stub makes the Fitter blow the device: "requires 8391-8437 LABs,
# but the device contains only 4191" (~2x over). Bisection (A/B reverting one
# new Task-3 signal at a time) isolated bgw_argb4444 as the SOLE trigger --
# c_bgcov_clear alone fits clean, matching the known-good baseline exactly
# (16,133 ALMs / 22,835 registers).
#
# The build log's mechanism: physical-synthesis register retiming reports
# "estimated slack improvement of 2267 ps" (a pre-existing, already-marginal
# path) immediately followed by "Created 281 register duplicates". Per-instance
# post-fit register counts show comp_pipeline (u_pipe) alone ballooning from a
# small fraction of the ~22,835-register baseline to 51,876 registers --
# despite this diff never touching comp_pipeline.sv. Reading bgw_argb4444 real
# apparently perturbs comp_pipeline's timing graph (shared fb_rd_qword /
# fb_rd_en read-mux fan-in -- comp_pipeline, fbram_snapshot(u_snap), and
# fbram_to_sdram(u_bgw) all read the same comp_fbram port) just enough that
# Solarus.qsf's global EXTRA-effort physical-synthesis settings (line ~45
# above: PHYSICAL_SYNTHESIS_EFFORT EXTRA + REGISTER_DUPLICATION/RETIMING ON)
# respond by duplicating registers through comp_pipeline until the design no
# longer fits.
#
# Rather than dial back those global settings (this project has separate,
# hard-won HW timing fixes elsewhere -- e.g. #44/#46 SDRAM read-capture,
# pll_hdmi -- that likely depend on this aggressive global posture), try
# exempting JUST the comp_pipeline instance from the two passes the log
# directly implicates (retiming's slack estimate -> register duplication).
# Hierarchy path confirmed from an actual post-fit report (not a guess):
#   emu:emu|blitter_top:blitter|comp_pipeline:u_pipe
# (sys_top -> emu:emu is Solarus.sv's `module emu`; blitter_top is
# instantiated there as `blitter_top blitter (...)` at Solarus.sv:624;
# comp_pipeline is instantiated inside blitter_top.sv:959 as `comp_pipeline
# u_pipe (...)`.)
#
# CONFIDENCE NOTE: PHYSICAL_SYNTHESIS_REGISTER_DUPLICATION/_RETIMING are
# documented Quartus assignment-editor entries under the "Physical Synthesis"
# category and I believe (from prior Quartus usage, not verified against
# Intel docs I don't have access to here) they accept instance-level -to
# scoping the same way the FAST_INPUT_REGISTER example above does, applying
# to every register within that instance's hierarchy. UNVERIFIED against
# Quartus 17.0's actual assignment set -- if either name is rejected or
# silently ignored (watch for an "unknown assignment" warning, or for
# comp_pipeline's register count in the post-fit report NOT dropping back
# toward baseline), that disproves per-instance scoping for this pass and a
# different lever (RTL-side false-path/multicycle on the actual violating
# path, or a global dial-back) is needed instead.
set_instance_assignment -name PHYSICAL_SYNTHESIS_REGISTER_DUPLICATION OFF -to "emu:emu|blitter_top:blitter|comp_pipeline:u_pipe" -entity sys_top
set_instance_assignment -name PHYSICAL_SYNTHESIS_REGISTER_RETIMING OFF -to "emu:emu|blitter_top:blitter|comp_pipeline:u_pipe" -entity sys_top

# Escalation candidates if the two assignments above don't fully resolve the
# overflow (comp_pipeline register count still inflated in the post-fit
# report) -- uncomment incrementally, re-testing after each, rather than all
# at once, so we know which one actually mattered:
#
# set_instance_assignment -name ROUTER_REGISTER_DUPLICATION OFF -to "emu:emu|blitter_top:blitter|comp_pipeline:u_pipe" -entity sys_top
# set_instance_assignment -name PHYSICAL_SYNTHESIS_COMBO_LOGIC OFF -to "emu:emu|blitter_top:blitter|comp_pipeline:u_pipe" -entity sys_top
# set_instance_assignment -name PHYSICAL_SYNTHESIS_ASYNCHRONOUS_SIGNAL_PIPELINING OFF -to "emu:emu|blitter_top:blitter|comp_pipeline:u_pipe" -entity sys_top

set_instance_assignment -name PARTITION_HIERARCHY root_partition -to | -section_id Top